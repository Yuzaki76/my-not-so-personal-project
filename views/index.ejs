<h2 class="page-header">Recently added</h2>
<div>
    <% books.forEach(book=> { %>
        <a href="/books/<%= book.id %> "><img width="100" height="150" src='<%= book.coverImagePath %>'></a>
    <% }) %>
</div>